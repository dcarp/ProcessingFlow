namespace ProcessingFlow.FlatBuffers;

table Node {
    name: string (key);
    type_url: string;
    input_type_url: string;
    parameters_type_url: string;
    output_type_url: string;
}

root_type Node;
